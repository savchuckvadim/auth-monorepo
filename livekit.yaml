port: 7880
bind_addresses:
  - "0.0.0.0"
rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: true


# keys:
#   LK_API_KEY: ${LIVEKIT_API_KEY}
#   LK_API_SECRET: ${LIVEKIT_API_SECRET}

room:
  auto_create: true
# Если Dockploy дает домен с SSL (например через Nginx/Traefik),
# LiveKit может работать за прокси.
